import { RevealPasswordDirective } from './reveal-password.directive';

describe('RevealPasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new RevealPasswordDirective();
    expect(directive).toBeTruthy();
  });
});
